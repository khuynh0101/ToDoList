import React from 'react';
import './ListItem.css';

const ListItem = (items) => {
  console.log(items);
  return (
  <div className="list">
      {/* {
        todoList.map(function(name, index){
          return name;
        })
      } */}
  </div>
  )
};

export default ListItem;
